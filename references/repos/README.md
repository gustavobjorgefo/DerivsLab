# Third-Party Repositories (Reference Only)

External repositories cloned here for reference are intentionally excluded
from version control (see root `.gitignore`). To restore this folder on a
new machine, clone the repositories listed below.

| Repository | URL | Notes |
|---|---|---|
| (add as you clone) | | |