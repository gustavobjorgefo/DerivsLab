# References

Local-only material used as reference during development. Not part of the
`derivslab` package and not distributed with it.

- `vba/` — legacy spreadsheets (.xlsm) with VBA functions used as a
  reference point for porting logic into Python.
- `repos/` — third-party repositories kept locally for inspiration.
  Cloned, never committed.